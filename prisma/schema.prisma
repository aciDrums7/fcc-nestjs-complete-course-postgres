generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL_PRISMA")
}

model Song {
  id    Int    @id @default(autoincrement())
  title String

  @@map("songs")
}
